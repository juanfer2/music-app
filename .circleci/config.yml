version: 2.1
orbs:
  node: circleci/node@1.1
jobs:
  test:
    executor:
      name: node/default
      tag: '10.4'
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
      - run: npm run test 
    #environment:
    #  -  SKIP_PREFLIGHT_CHECK: 'true'
    #  -  REACT_APP_ENVIROMENT: 'test'
